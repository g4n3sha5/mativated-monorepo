{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "dev": {
      "outputs": ["pnpm run dev", "pnpm run server"]
    },
    "type-check": {}
  }
}
